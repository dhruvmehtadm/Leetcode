class Solution:
    def average(self, salary: List[int]) -> float:
        a = max(salary)
        b = min(salary)
        n = len(salary)
        avg = (sum(salary) - a - b)/(n-2)
        return avg
