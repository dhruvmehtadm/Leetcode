class Solution:
    def arraySign(self, nums: List[int]) -> int:
        if 0 in nums:
            return 0
        else:
            num = 1
            for i in nums:
                num = num*i
            if(num>0):
                return 1
            else:
                return -1
