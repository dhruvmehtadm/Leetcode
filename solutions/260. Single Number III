class Solution:
    def singleNumber(self, nums: List[int]) -> List[int]:
        single = []
        for i in nums:
            if(nums.count(i)==1):
                single.append(i)
        return single
