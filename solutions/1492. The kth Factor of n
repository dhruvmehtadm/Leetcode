class Solution:
    def kthFactor(self, n: int, k: int) -> int:
        out = []
        for i in range(1, n+1):
            if(n%i==0):
                out.append(i)
        if(k>len(out)):
            return -1
        else:
            for i in range(len(out)):
                return out[k-1]
