class Solution:
    def findGCD(self, nums: List[int]) -> int:
        divisors = []
        for i in range(1, min(nums)+1):
            if(min(nums)%i==0 and max(nums)%i==0):
                divisors.append(i)
        return max(divisors)
