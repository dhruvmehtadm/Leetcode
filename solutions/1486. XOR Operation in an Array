class Solution:
    def xorOperation(self, n: int, start: int) -> int:
        nums = []
        xor = 0
        for i in range(n):
            nums.append(start + (2*i))
            xor = xor^nums[i]
        return xor
