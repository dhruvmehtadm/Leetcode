class Solution:
    def isPowerOfThree(self, n: int) -> bool:
        l = []
        while(n>=1):
            if(n>=1):
                l.append(n%3)
            n = n//3
        if(sum(l)==1):
            return True
        else:
            return False
