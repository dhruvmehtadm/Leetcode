class Solution:
    def sumOfUnique(self, nums: List[int]) -> int:
        a = []
        for i in nums:
            if(nums.count(i)==1):
                a.append(i)
        b = sum(a)
        return b
